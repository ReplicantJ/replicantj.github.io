# .env.local (gitignored) — optional overrides

VITE_API_BASE_URL=
VITE_BASE_PATH=/
# Discord profile link: Settings → App Settings → Advanced → enable Developer Mode →
# right-click your avatar → Copy User ID (17–20 digit number). Usernames break https://discord.com/users/… links.
VITE_DISCORD_USER_ID=
